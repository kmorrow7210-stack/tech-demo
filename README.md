# tech-demo

Sticky monorepo for Tech Demos.

Each pick lives under `apps/<slug>/`. Use Bun. Plan with `skills/project-planning`. Every demo PR should include at least one screenshot and one video of the running app.
